apiVersion: apps/v1
kind: Deployment
metadata:
  name: ipam-controller-manager
  namespace: ipam-provider-m3-system
spec:
  template:
    spec:
      containers:
      - name: manager
        imagePullPolicy: IfNotPresent